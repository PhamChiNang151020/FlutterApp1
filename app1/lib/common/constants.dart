import 'package:flutter/material.dart';

//Màu sắc
const primaryColor = Color.fromARGB(255, 57, 128, 244);
const btnColor = Colors.amber;
const defaultColor = Color.fromARGB(255, 236, 240, 250);
const titleMenuColor = Color.fromARGB(255, 50, 63, 101);

const defaultPadding = 20.0;

OutlineInputBorder textFieldBorder = OutlineInputBorder(
  borderSide: BorderSide(color: primaryColor.withOpacity(0.1)),
);
